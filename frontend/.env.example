# NestJS backend (Edu-backend) — global prefix is "/api", set in src/main.ts
# For local dev against `npm run start:dev` in Edu-backend, use:
# VITE_API_BASE_URL=http://localhost:7001/api
VITE_API_BASE_URL=http://edu-najottalim.uz/api
